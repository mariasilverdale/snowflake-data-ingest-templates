USE DATABASE INGEST_DB;

-- CSV format (header row, standard quoting)
CREATE OR REPLACE FILE FORMAT OPS.FF_CSV_STD
  TYPE = CSV
  FIELD_DELIMITER = ','
  SKIP_HEADER = 1
  FIELD_OPTIONALLY_ENCLOSED_BY = '"'
  NULL_IF = ('', 'NULL', 'null')
  EMPTY_FIELD_AS_NULL = TRUE
  TRIM_SPACE = TRUE;

-- JSON format (standard)
CREATE OR REPLACE FILE FORMAT OPS.FF_JSON
  TYPE = JSON;
